# Production Frontend Environment Variables
# If using Nginx reverse proxy on the same domain, keep /api
# If backend is on a separate domain (e.g. https://api.yourdomain.com), set full URL
VITE_API_URL=https://ambiguous-approve-thicken.ngrok-free.dev/api
